import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  input: {
    marginTop: 20,
    marginHorizontal: 20,
    borderWidth: 1,
    borderRadius: 3,
    paddingHorizontal: 19,
    paddingVertical: 10
  }
})